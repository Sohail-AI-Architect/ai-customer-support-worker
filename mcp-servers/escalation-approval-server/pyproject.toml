[project]
name = "escalation-approval-server"
version = "0.1.0"
description = "MCP server exposing worker-initiated escalation.create and approval.request tools (US4/US5)"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["src", "../../backend/src"]
testpaths = ["tests"]
