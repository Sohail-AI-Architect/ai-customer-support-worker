# Copy to .env for local dev. Never commit .env.
# Backend API base used by the dev proxy (next.config.mjs rewrites).
BACKEND_URL=http://localhost:8000

# Demo customer id sent as X-Customer-Id until real session auth (plan Section 16).
NEXT_PUBLIC_CUSTOMER_ID=demo-customer-1
